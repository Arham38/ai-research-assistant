# PHASE 6: POST /auth/register, POST /auth/login — JWT issuance
from fastapi import APIRouter

router = APIRouter(prefix="/auth", tags=["auth"])

# TODO: @router.post("/register"), @router.post("/login")
