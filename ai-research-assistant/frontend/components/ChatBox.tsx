// PHASE 3: chat UI scoped to one paper, streams answer via SSE from /chat/{paperId}

export default function ChatBox({ paperId }: { paperId: string }) {
  return <div>TODO: ChatBox for paper {paperId}</div>;
}
