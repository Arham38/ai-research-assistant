// PHASE 2: paper detail + structured summary
// TODO: fetch summary from POST {API_BASE_URL}/summarize/{id}
// render collapsible sections: problem, methodology, results, limitations

export default function PaperDetailPage({ params }: { params: { id: string } }) {
  return <div>TODO: Phase 2 — summary view for paper {params.id}</div>;
}
