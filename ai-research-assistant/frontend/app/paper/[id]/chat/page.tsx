// PHASE 3: RAG chat with this specific paper
// TODO: render <ChatBox paperId={params.id} />
// streams from POST {API_BASE_URL}/chat/{id} via SSE

export default function PaperChatPage({ params }: { params: { id: string } }) {
  return <div>TODO: Phase 3 — chat UI for paper {params.id}</div>;
}
