// PHASE 4: personal library — saved papers, tags, notes
// TODO: GET {API_BASE_URL}/library, tag/note actions, "select to compare"

export default function LibraryPage() {
  return <div>TODO: Phase 4 — library grid/list</div>;
}
