// Landing page — links into /search, /library, /compare, /lit-review

export default function HomePage() {
  return (
    <main className="flex flex-col items-center justify-center p-12">
      <h1 className="text-2xl font-medium">AI Research Assistant</h1>
      <p className="text-gray-500 mt-2">
        TODO: hero section + nav into search / library / lit-review
      </p>
    </main>
  );
}
